name: 🏗️ Build DAWAI APK

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

permissions:
  contents: write

jobs:
  build:
    name: Build Debug APK
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: temurin

      - name: 📦 Install dependencies
        run: npm install --legacy-peer-deps

      - name: 🩹 Patch ReactSettingsExtension bug
        run: |
          FILE="node_modules/@react-native/gradle-plugin/settings-plugin/src/main/kotlin/com/facebook/react/ReactSettingsExtension.kt"
          [ -f "$FILE" ] && sed -i 's/.readText().substring(0, 1024)/.readText().take(1024)/g' "$FILE" && echo "✅ Patched"

      - name: 🗺️ Fix react-native-maps pre-generated ViewManager interfaces
        run: |
          # react-native-maps ships pre-generated Java interface files compiled against
          # an older RN codegen API. Delete them so RN 0.76.9 codegen regenerates them correctly.
          VIEWMANAGERS_DIR="node_modules/react-native-maps/android/src/main/java/com/facebook/react/viewmanagers"
          if [ -d "$VIEWMANAGERS_DIR" ]; then
            COUNT=$(ls "$VIEWMANAGERS_DIR"/*.java 2>/dev/null | wc -l)
            rm -f "$VIEWMANAGERS_DIR"/*.java
            echo "✅ Deleted $COUNT pre-generated ViewManager interface files from react-native-maps"
            ls "$VIEWMANAGERS_DIR" 2>/dev/null || echo "  Directory now empty"
          else
            echo "ℹ️ ViewManagers directory not found"
          fi

      - name: 🔧 Patch codegen specs (undefined prop types)
        run: |
          find node_modules -type f \( -name "*.ts" -o -name "*.tsx" -o -name "*.js" \) 2>/dev/null \
            | xargs grep -rl "accessibilityContainerViewIsModal\|WithDefault.*undefined" 2>/dev/null \
            | while read f; do
                sed -i '/accessibilityContainerViewIsModal/d' "$f"
                sed -i 's/WithDefault<boolean, undefined>/boolean/g' "$f"
                sed -i 's/WithDefault<string, undefined>/string/g' "$f"
              done
          echo "✅ Codegen spec patches done"

      - name: 🤖 Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          accept-android-sdk-licenses: true

      - name: 📱 Install SDK packages
        run: |
          yes | sdkmanager --licenses > /dev/null 2>&1 || true
          sdkmanager "platforms;android-35" "build-tools;35.0.0" "ndk;27.1.12297006"

      - name: 🔥 Setup Firebase
        run: |
          if [ -n "${{ secrets.GOOGLE_SERVICES_JSON }}" ]; then
            echo '${{ secrets.GOOGLE_SERVICES_JSON }}' > android/app/google-services.json
          else
            echo "⚠️ GOOGLE_SERVICES_JSON غير موجود"
          fi

      - name: 🗺️ Maps API Key
        run: |
          if [ -n "${{ secrets.MAPS_API_KEY }}" ]; then
            sed -i 's/YOUR_MAPS_API_KEY/${{ secrets.MAPS_API_KEY }}/g' android/app/src/main/AndroidManifest.xml
          fi

      - name: 🔤 Copy fonts
        run: |
          mkdir -p android/app/src/main/assets/fonts
          cp node_modules/react-native-vector-icons/Fonts/*.ttf android/app/src/main/assets/fonts/ 2>/dev/null || true

      - name: 🔑 Debug Keystore
        run: |
          keytool -genkeypair -v -keystore android/app/debug.keystore -alias androiddebugkey \
            -keyalg RSA -keysize 2048 -validity 10000 -storepass android -keypass android \
            -dname "CN=Android Debug,O=Android,C=US"

      - name: 🛠️ local.properties
        run: echo "sdk.dir=$ANDROID_HOME" > android/local.properties

      - name: 🏗️ Build Debug APK
        working-directory: android
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon --stacktrace 2>&1 | tee /tmp/build.log
          BUILD_EXIT=${PIPESTATUS[0]}
          if [ $BUILD_EXIT -ne 0 ]; then
            echo "=== KEY ERRORS ==="
            grep -E "(^error:|FAILED$|Caused by:|cannot find symbol|does not exist|Exception:)" \
              /tmp/build.log | grep -v "^$" | head -30 || true
            exit $BUILD_EXIT
          fi
          echo "✅ BUILD SUCCESS"
          ls -lh app/build/outputs/apk/debug/

      - name: 💾 Save build error to repo
        if: failure()
        run: |
          mkdir -p debug
          {
            echo "=== Run #${{ github.run_number }} ==="
            echo "=== KEY ERRORS ==="
            grep -E "(^error:|FAILED$|Caused by:|cannot find symbol|does not exist|Exception:)" \
              /tmp/build.log | head -50 || true
            echo ""
            echo "=== LAST 80 LINES ==="
            tail -80 /tmp/build.log
          } > build-error.log
          git config user.email "ci@dawai.app"
          git config user.name "CI Bot"
          git add build-error.log
          git commit -m "ci: build error #${{ github.run_number }}" || true
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main || true

      - name: 📤 Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: DAWAI-debug-v${{ github.run_number }}
          path: android/app/build/outputs/apk/debug/*.apk
          retention-days: 30

      - name: 📊 Summary
        if: always()
        run: |
          if [ -f android/app/build/outputs/apk/debug/app-debug.apk ]; then
            SIZE=$(du -sh android/app/build/outputs/apk/debug/app-debug.apk | cut -f1)
            echo "✅ APK جاهز — $SIZE" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ فشل البناء" >> $GITHUB_STEP_SUMMARY
          fi
